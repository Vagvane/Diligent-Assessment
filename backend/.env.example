# backend/.env.example
# Copy to .env and update values. Never commit real secrets.

PORT=4000
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/shopsphere?retryWrites=true&w=majority
CLIENT_ORIGIN=http://localhost:5173

# Optional: allow overriding database name used by Mongoose
# MONGODB_DB=shopsphere

